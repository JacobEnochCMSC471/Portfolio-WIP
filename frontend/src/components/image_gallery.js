import React from "react";

/* Not going to work - images need to be either imported individually or supplied from a backend server

const base_path = '..src/images/mb_photos/'
const base_photo_name = 'img_'

var photo_paths = []

for (let i = 0; i < 26; i++){
    curr_photo = base_path + base_photo_name + i
    photo_paths.push(curr_photo)
}

*/
const ImageGallery = () => {

    return (
        <div class="gallery">

        </div>
    );
}

export default ImageGallery;